import { createLookupCollection } from './createLookupCollection'

export const Necklines = createLookupCollection({
  slug: 'necklines',
  singularLabel: 'Neckline',
  pluralLabel: 'Necklines',
  description: 'Dress neckline attributes',
})
